input {
        beats {
                port => 5044
        }
}
output {
        stdout { }
        elasticsearch {
                hosts => [{{range $index, $service := service "elasticsearch-9200"}}{{if ne $index 0}},{{end}}"{{.Address}}"{{end}}]
        }
}
